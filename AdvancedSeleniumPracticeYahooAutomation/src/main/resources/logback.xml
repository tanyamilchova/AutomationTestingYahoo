<!--<configuration>-->

<!--    &lt;!&ndash; Define the rolling policy &ndash;&gt;-->
<!--    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">-->
<!--        <file>logs/application.log</file>-->

<!--        &lt;!&ndash; Set the daily rolling policy &ndash;&gt;-->
<!--        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">-->
<!--            <fileNamePattern>logs/%d{yyyy-MM-dd}-application.log</fileNamePattern>-->
<!--            <maxHistory>30</maxHistory>-->
<!--        </rollingPolicy>-->

<!--        &lt;!&ndash; Define the log pattern &ndash;&gt;-->
<!--        <encoder>-->
<!--            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>-->
<!--        </encoder>-->
<!--    </appender>-->

<!--    &lt;!&ndash; Define the root logger &ndash;&gt;-->
<!--    <root level="INFO">-->
<!--        <appender-ref ref="FILE" />-->
<!--    </root>-->
<!--</configuration>-->
<configuration>

    <!-- Define the rolling policy -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/application.log</file>

        <!-- Set the daily rolling policy -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/%d{yyyy-MM-dd}-application.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>

        <!-- Define the log pattern -->
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Define the root logger -->
    <root level="INFO">
        <appender-ref ref="FILE" />
    </root>

    <!-- Define a logger for the MailPage class with DEBUG level -->
    <logger name="com.example.MailPage" level="DEBUG" additivity="false">
        <appender-ref ref="FILE" />
    </logger>

</configuration>
